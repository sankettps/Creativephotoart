<table>
<tbody>
	<tr>
		<td><%= link_to "Apps", apps_path %></td>
		<td><%= link_to "App Adds", app_adds_path %></td>
		<td><%= link_to "App Frames", app_frames_path %></td>
		<% if user_signed_in?%>
			<td><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></td>
		<% else %>
			<td><%= link_to "Sign In", new_user_session_path %></td>
		<% end %>
	</tr>
</tbody>
</table>